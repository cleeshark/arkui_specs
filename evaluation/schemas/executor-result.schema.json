{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "executor-result.schema.json",
  "title": "Semantic Executor Final Result",
  "description": "Strict envelope written by the Codex CLI executor (--output-last-message) for one staged work item. observation_json carries only the serialized executor-owned observation or aggregation payload; the service merges it into the initialized staged template and validates the candidate separately against the staged-run contract.",
  "type": "object",
  "required": [
    "schema_version",
    "work_item_id",
    "status",
    "observation_json",
    "notes",
    "error"
  ],
  "properties": {
    "schema_version": { "type": "integer", "const": 2 },
    "work_item_id": { "type": "string", "minLength": 1 },
    "status": {
      "type": "string",
      "enum": ["completed", "failed"]
    },
    "observation_json": { "type": ["string", "null"] },
    "notes": {
      "type": "array",
      "items": { "type": "string" }
    },
    "error": { "type": ["string", "null"] }
  },
  "additionalProperties": false
}
